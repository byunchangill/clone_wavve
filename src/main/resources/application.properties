# server port change
server.port = 8090

# mariaDB connection
spring.datasource.driverClassName=org.mariadb.jdbc.Driver
spring.datasource.url=jdbc:mariadb://localhost:3308/wavve_clone
spring.datasource.username=root
spring.datasource.password=koreait

#mybatis
mybatis.mapper-locations=mappers/**/*.xml
mybatis.type-aliases-package=com.project.clone_wavve